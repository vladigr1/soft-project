1) model name should be saved as the name of then model when generate model 

2) number of days should be a droptext and will be needed for generate model

3)  *units should be fixed so they cannot change

4) status needs to be droptext between basic/derived/empty=not used

5) generate model = change text to save model

6) generate model needs to be implemented = save the model in the system (JSON file) - vlad

7) generate form - needs to be implemented = take saved data model and generate the appopriate form 

8) in setup and in generate model refresh the list of models get models from JSON files.

** make sure that when you create the data model class you add field of ID so diffrente models can have the same name.